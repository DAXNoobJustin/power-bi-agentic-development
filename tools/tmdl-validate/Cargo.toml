[package]
name = "tmdl-validate"
version = "0.1.0"
edition = "2024"
description = "Structural validator for TMDL (Tabular Model Definition Language) files"

[dependencies]

[profile.release]
opt-level = "s"
lto = true
strip = true
